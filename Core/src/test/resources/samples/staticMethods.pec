define name as Text attribute
define Parent as category with attribute name, and methods:

	@Static
	define staticMethod as method doing:
		return
	
define Child as Parent with methods:

	define staticMethod as method doing:
		return
